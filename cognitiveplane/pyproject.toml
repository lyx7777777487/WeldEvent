[project]
name = "cognitiveplane"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "fastapi>=0.100",
    "uvicorn>=0.23",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
# L1→L2 桥接可选依赖（boundary-pinning §6.2）
# 启用 Temporal 接入时安装: pip install -e ".[temporal]"
temporal = [
    "temporalio>=1.27.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["cognitiveplane"]

[tool.setuptools.package-dir]
cognitiveplane = "."

[tool.pytest.ini_options]
asyncio_mode = "auto"
