"""PostgreSQL adapter package — Phase 1 stub.

Real implementations land in Phase 2 (alembic migrations, SQLAlchemy 2.x
async ORM models, async repository implementations). For now this module
provides structural placeholders that other code can import without runtime
errors.
"""
